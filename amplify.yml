version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            - npm i -g yarn
            - yarn install --frozen-lockfile
        build:
          commands:
            - yarn build
      artifacts:
<<<<<<< HEAD
        # Upload built app (client + SSR bundle) into dist/
>>>>>>> b858517d16f80acaa62e93f8727c0e62e2a628c4
        baseDirectory: dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    customHeaders:
      - pattern: "**/*"
        headers:
          - key: Strict-Transport-Security
            value: max-age=63072000; includeSubDomains; preload
