# Server
PORT=5173
BASE=/
NODE_ENV=development

# App
VITE_APP_NAME=TeaForms

# AWS (future; leave unset locally)
# AWS_REGION=
# DDB_TABLE=
# COGNITO_USER_POOL_ID=
# COGNITO_CLIENT_ID=
# JWT_ROLE_CLAIM=custom:role
# To enable DynamoDB repositories (with DynamoDB Local):
# USE_DYNAMODB=true
# DDB_ENDPOINT=http://localhost:8000
# DDB_CREATE_TABLES=false

# AWS (prod/local)
# AWS_REGION=eu-west-2
# (Local dev) export AWS_PROFILE=cgtaa and AWS_SDK_LOAD_CONFIG=1

# Sessions
# SESSION_SECRET=change_me
